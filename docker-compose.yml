services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      # Default environment setting, can be overridden
      - ENVIRONMENT=production
    # The default CMD is defined in the Dockerfile
